 // Contenido para: .sequelizerc

const path = require('path');

module.exports = {
  'config': path.resolve('config', 'config.json'), // Ruta al archivo de configuración de la BD
  'models-path': path.resolve('models'),           // Ruta a la carpeta de modelos
  'seeders-path': path.resolve('seeders'),         // Ruta a la carpeta de seeders
  'migrations-path': path.resolve('migrations')    // Ruta a la carpeta de migraciones
};


